# Variables and assignment
x <- 42
y <- 3.14
name <- "Alice"

# Functions
greet <- function(name) {
    paste("Hello,", name, "!")
}

result <- greet(name)

# Vectors
numbers <- c(1, 2, 3, 4, 5)
doubled <- numbers * 2

# Data frames
df <- data.frame(
    name = c("Alice", "Bob"),
    age = c(30, 25)
)
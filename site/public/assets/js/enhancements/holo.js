(function(){'use strict';const log=window.BengalUtils?.log||(()=>{});const ready=window.BengalUtils?.ready||((fn)=>{if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',fn);}else{fn();}});const CONFIG={maxRotation:15,resetDuration:400,throttleMs:16,selectors:{holoCard:'.holo-card',holoAdmonition:'.admonition.holo',all:'.holo-card, .admonition.holo'}};let rafId=null;let lastMoveTime=0;let domObserver=null;let holoObserver=null;function initAdmonitionElement(el){if(el.dataset.holoInit)return;el.dataset.holoInit='true';el.addEventListener('mousemove',handleAdmonitionMouseMove);el.addEventListener('mouseleave',handleAdmonitionMouseLeave);}
function handleAdmonitionMouseMove(e){const rect=this.getBoundingClientRect();const x=((e.clientX-rect.left)/rect.width)*100;const y=((e.clientY-rect.top)/rect.height)*100;const clampedX=Math.max(0,Math.min(100,x));const clampedY=Math.max(0,Math.min(100,y));this.style.setProperty('--holo-x',clampedX.toFixed(1));this.style.setProperty('--holo-y',clampedY.toFixed(1));}
function handleAdmonitionMouseLeave(){this.style.setProperty('--holo-x','50');this.style.setProperty('--holo-y','50');}
function getPointerPosition(event,element){const rect=element.getBoundingClientRect();const x=event.clientX-rect.left;const y=event.clientY-rect.top;const percentX=Math.max(0,Math.min(100,(x/rect.width)*100));const percentY=Math.max(0,Math.min(100,(y/rect.height)*100));return{x:percentX,y:percentY};}
function getRotation(percentX,percentY){const normalX=(percentX/50)-1;const normalY=(percentY/50)-1;return{x:normalY*CONFIG.maxRotation,y:normalX*CONFIG.maxRotation};}
function updateCardStyles(element,percentX,percentY){const rotation=getRotation(percentX,percentY);element.style.setProperty('--pointer-x',percentX.toFixed(2));element.style.setProperty('--pointer-y',percentY.toFixed(2));element.style.setProperty('--rotate-x',`${rotation.x.toFixed(2)}deg`);element.style.setProperty('--rotate-y',`${rotation.y.toFixed(2)}deg`);element.style.setProperty('--bg-x',`${percentX}%`);element.style.setProperty('--bg-y',`${percentY}%`);}
function resetCardStyles(element){element.style.setProperty('--pointer-x','50');element.style.setProperty('--pointer-y','50');element.style.setProperty('--rotate-x','0deg');element.style.setProperty('--rotate-y','0deg');element.style.setProperty('--bg-x','50%');element.style.setProperty('--bg-y','50%');element.classList.remove('active');}
function handleCardMouseEnter(event){const element=event.currentTarget;element.classList.add('active');}
function handleCardMouseMove(event){const now=performance.now();if(now-lastMoveTime<CONFIG.throttleMs)return;lastMoveTime=now;const element=event.currentTarget;if(rafId){cancelAnimationFrame(rafId);}
rafId=requestAnimationFrame(()=>{const pos=getPointerPosition(event,element);updateCardStyles(element,pos.x,pos.y);});}
function handleCardMouseLeave(event){const element=event.currentTarget;if(rafId){cancelAnimationFrame(rafId);rafId=null;}
resetCardStyles(element);}
function handleTouchStart(event){const element=event.currentTarget;element.classList.add('active');const touch=event.touches[0];const pos=getPointerPosition(touch,element);updateCardStyles(element,pos.x,pos.y);}
function handleTouchMove(event){const element=event.currentTarget;const touch=event.touches[0];if(rafId){cancelAnimationFrame(rafId);}
rafId=requestAnimationFrame(()=>{const pos=getPointerPosition(touch,element);updateCardStyles(element,pos.x,pos.y);});}
function handleTouchEnd(event){const element=event.currentTarget;resetCardStyles(element);}
function initCard(element){if(element.dataset.holoInit)return;element.dataset.holoInit='true';resetCardStyles(element);element.addEventListener('mouseenter',handleCardMouseEnter);element.addEventListener('mousemove',handleCardMouseMove);element.addEventListener('mouseleave',handleCardMouseLeave);element.addEventListener('touchstart',handleTouchStart,{passive:true});element.addEventListener('touchmove',handleTouchMove,{passive:true});element.addEventListener('touchend',handleTouchEnd,{passive:true});}
function init(){const cards=document.querySelectorAll(CONFIG.selectors.holoCard);cards.forEach(initCard);const admonitions=document.querySelectorAll(CONFIG.selectors.holoAdmonition);admonitions.forEach(initAdmonitionElement);log('[BengalHolo] Initialized');}
function initElement(el){if(el.matches(CONFIG.selectors.holoCard)){initCard(el);}else if(el.matches(CONFIG.selectors.holoAdmonition)){initAdmonitionElement(el);}}
function cleanup(){if(domObserver){domObserver.disconnect();domObserver=null;}
if(holoObserver){holoObserver.disconnect();holoObserver=null;}
const allElements=document.querySelectorAll(CONFIG.selectors.all);allElements.forEach((element)=>{element.removeEventListener('mouseenter',handleCardMouseEnter);element.removeEventListener('mousemove',handleCardMouseMove);element.removeEventListener('mouseleave',handleCardMouseLeave);element.removeEventListener('touchstart',handleTouchStart);element.removeEventListener('touchmove',handleTouchMove);element.removeEventListener('touchend',handleTouchEnd);element.removeEventListener('mousemove',handleAdmonitionMouseMove);element.removeEventListener('mouseleave',handleAdmonitionMouseLeave);delete element.dataset.holoInit;});}
function observeDOM(){if(domObserver)return domObserver;let pendingNodes=[];let debounceTimer=null;function processPendingNodes(){const nodes=pendingNodes;pendingNodes=[];debounceTimer=null;nodes.forEach((node)=>{if(node.matches&&node.matches(CONFIG.selectors.all)){initElement(node);}
const children=node.querySelectorAll?.(CONFIG.selectors.all);if(children){children.forEach(initElement);}});}
domObserver=new MutationObserver((mutations)=>{mutations.forEach((mutation)=>{mutation.addedNodes.forEach((node)=>{if(node.nodeType!==Node.ELEMENT_NODE)return;pendingNodes.push(node);});});if(pendingNodes.length>0&&!debounceTimer){debounceTimer=setTimeout(processPendingNodes,50);}});domObserver.observe(document.body,{childList:true,subtree:true});return domObserver;}
if(window.Bengal&&window.Bengal.enhance){Bengal.enhance.register('holo',init);}
window.BengalHolo={init:init,initElement:initElement,cleanup:cleanup,};window.HoloCards={init:init,initCard:initCard,cleanup:cleanup,config:CONFIG};ready(()=>{init();observeDOM();});document.addEventListener('turbo:load',init);document.addEventListener('pjax:end',init);})();
# Production Environment
# =======================

site:
  # GitHub Pages project site: deployed at /rosettes
  baseurl: "/rosettes"

build:
  incremental: true
  strict_mode: true

assets:
  minify: true
  fingerprint: true


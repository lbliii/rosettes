# Production Environment
# =======================

site:
  # GitHub Pages project site: deployed at /rosettes
  baseurl: "/rosettes"

build:
  incremental: false  # CI runs are clean slate, no cache
  strict_mode: true

assets:
  minify: true
  fingerprint: true

